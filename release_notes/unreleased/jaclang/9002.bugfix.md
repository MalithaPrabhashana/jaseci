- **Fix: `media.impl.jac` no longer fails `jac check` where pillow is installed** (#9002): PIL types `Image.open()` as `ImageFile`, whose inherited `.format` and `.save` the checker cannot see, so the file reported 13 errors. The handles are annotated; behaviour is unchanged.
