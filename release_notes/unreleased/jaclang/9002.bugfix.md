- **Fix: byLLM's `Image` no longer trips `jac check` when pillow is installed** (#9002): PIL types `Image.open()` as `ImageFile`, whose inherited `.format` and `.save` the checker cannot see, so `Image.postinit` reported 13 errors. The handles are annotated; encoding behaviour is unchanged.
