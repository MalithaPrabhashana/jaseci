- **Breaking: byLLM's `MockLLM` now parses replies like a real model**: it replaces only the network call, so queue typed answers as values (`36`, not `"36"`) and offer the tools a `MockToolCall` names.
